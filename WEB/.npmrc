# Vite 8 (pulled in transitively by @quasar/app-vite) is newer than the peer
# ranges some dev-only test tooling declares. legacy-peer-deps keeps the
# install deterministic instead of failing on ERESOLVE for tooling that is
# never part of the production build.
legacy-peer-deps=true
